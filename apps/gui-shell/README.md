# EMC Locus GUI Shell

This is the first static operator-facing shell for EMC Locus.

Open `index.html` directly in a browser. The current data is local fixture data;
the shell is intended to be wired to the Python/Rust application services once
those APIs are exposed.
