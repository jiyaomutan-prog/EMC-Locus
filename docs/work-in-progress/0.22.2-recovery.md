# Recovery journal - EMC Locus 0.22.2

Date: 2026-08-05

## Baseline inspected

- Repository: `jiyaomutan-prog/EMC-Locus`
- Main SHA: `9b4792984579f7a38143446e6bf958927c53bd70`
- Initial feature SHA: `9b4792984579f7a38143446e6bf958927c53bd70`
- Initial version: `0.22.1`
- Branch: `feature/0.22.2-method-workflow-and-control-ux`
- Working tree at start: clean
- `data/local-agent`: unchanged and excluded from all work

## Current contracts

- Test method definition: `emc-locus.test-template-definition.v1`
- Test method storage: `test_template_identities`, revision aggregate, schema version 5
- Station setup definitions: historical v1, v2, current v3
- Station storage: schema version 4
- Planned preparation: `emc-locus.planned-test-preparation.v1`
- Project storage: schema version 8

## Confirmed overlaps and gaps

- Method `instrumentation_chain` and station `material_requirements` both describe resource needs.
- Method slots are flat and less expressive than station v3 requirements.
- Station v3 correctly separates requirements from physical assignments, but the reusable logical topology is mixed with dated location and planned-use context.
- Planned preparation already owns dated revision pins and physical assignments; it must remain the single operational assignment path.
- Method sequence, limits and post-processing are typed but too shallow for hierarchical procedures, regulation, classified limits and an explainable dry-run plan.
- LAB CONSOLE exposes schema-shaped tabs and several raw internal enum values.
- The method library is flat and has no editable hierarchy.
- No first-class measurement-system template or regulation-profile aggregate exists.
- Python and Qt expose station views but no authoritative method/topology/execution-plan read projection.

## Target architecture

1. A method v2 owns reusable intent: typed variables, functional roles, procedure,
   sub-ranges, modulation, limits, post-processing and references to reusable
   topology/regulation revisions.
2. A measurement-system-template v1 owns reusable role nodes, logical ports,
   typed edges, correction points and feedback-loop mappings without date,
   location or physical asset.
3. A dated execution-configuration v1 extends planned preparation and pins the
   approved method revision, topology revision, parameter values and exact
   station/material snapshots.
4. The Rust Local Agent validates graphs, expressions, units, role mapping and
   compiles the read-only execution plan.
5. Station v2/v3 and preparation v1 remain readable. No historical checksum is
   recalculated.

## Compatibility strategy

- Add successor schemas; never edit approved v1 method definitions.
- Keep existing method routes for the revision aggregate and add explicit
  successor derivation plus dedicated hierarchy/topology/preview routes.
- Preserve station v2/v3 as dated operational realizations.
- Map legacy instrumentation slots to v2 functional roles only through an
  audited successor-draft command.
- Preserve unconvertible legacy fields as migration evidence requiring review.
- Add category aliases instead of rewriting immutable model revisions.
- No dual physical-assignment engine and no client-side compatibility engine.

## Planned additive migrations

- `test_definitions/0006_method_workflow_v2.sql`
- `test_definitions/0007_measurement_system_templates.sql`
- `test_definitions/0008_regulation_profiles.sql`
- `projects/0009_execution_configurations.sql`
- `equipment/0010_frequency_selective_category_alias.sql`

Exact table layout may be consolidated when transactional ownership makes a
single migration safer. Published migrations remain untouched.

## Checkpoints

- [x] Audit current method/station/preparation workflow
- [x] Core method v2, hierarchy and deterministic expressions
- [x] Core topology, regulation, procedure and execution-plan compiler
- [x] Agent persistence, routes, audit and outbox
- [x] Guided LAB CONSOLE workflow and accessible topology editor
- [x] Dated preparation handoff and category consolidation
- [x] Python/Qt read projections
- [ ] Full tests, UX evidence and release 0.22.2
- [ ] Pull request CI, merge commit and exact-main CI

## Validation commands

- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- repository Python compilation and unit tests
- LAB CONSOLE `npm ci`, typecheck, lint, Vitest, build and Playwright
- dedicated 0.22.2 real-agent Playwright suite
- `scripts/validate-ci.ps1 -NoInstall`
- `scripts/validate-historical-release-screenshots.ps1`
- `git diff --check`
- `git diff --cached --check`

## Current readiness

Core contracts, Local Agent persistence and the focused LAB CONSOLE workflow
are implemented. Validation passes the complete 110-test Agent library suite
and 53 LAB CONSOLE plus 194 Python tests, including explicit v1-to-v2 conversion, isolated
source failures, workflow CAS/idempotence, audit/outbox, category aliases,
accessible topology and dated execution derivation from the authoritative
0.22.1 preparation. Visual inspection at 1280 px confirmed that a hierarchy
failure remains local and leaves the method library readable.

## Exact resume instructions

1. Switch to `feature/0.22.2-method-workflow-and-control-ux`.
2. Confirm the tree state and inspect the latest commit.
3. Read this journal and
   `docs/domain/0.22.2-method-station-workflow-audit.md`.
4. Resume with Python/Qt read projections, then dedicated real-agent
   Playwright coverage, release documentation and the complete release gate.
   Do not modify historical schema constants or `data/local-agent`.
