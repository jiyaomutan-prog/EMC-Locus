# 0.22.0 Equipment Fleet and Operator Clarity - Recovery

## Baseline

- repository: `jiyaomutan-prog/EMC-Locus`;
- baseline branch: `main`;
- baseline release: `0.21.1`;
- baseline commit: `471e656ab007c1d627456a8dc90d9e3064074f35`;
- baseline CI: workflow `CI`, run number `130`, run ID `30201188920`,
  conclusion `success`;
- pull request `#2` was merged with a merge commit;
- local `main` and `origin/main` matched and the working tree was clean before
  branch creation on 2026-07-26;
- operator storage `data/local-agent` must remain untouched.

## Current branch

`feature/0.22.0-equipment-fleet-ux-clarity`

## Release objective

Release `0.22.0` establishes a clear laboratory boundary between:

1. a generic **Modèle constructeur**;
2. an **Exemplaire du parc** actually used by the laboratory;
3. the optional **Métrologie du parc** records attached to that item.

The release also introduces an independent laboratory-location registry,
hierarchical catalogue and fleet navigation, physical-item selectors for
station setup and test preparation, operator-oriented French vocabulary, and
isolated panel loading failures.

## Current phase

External integrity corrections 1 through 8 are implemented. Corrections 1
through 7 are published through coherent checkpoints; correction 8 is locally
validated and is the current publication checkpoint. The next slice is the
complete real-agent evidence pass before current-release screenshots and the
version bump.

`equipment.sqlite/physical_assets` is the only new-write source of truth
for physical assets and `equipment.sqlite/laboratory_locations` is the location
registry. Every successful command executes its conflict checks, write, audit
and sync outbox insertion under one `BEGIN IMMEDIATE` boundary. Metrology keeps
only dossiers and evidence referencing these assets. Its former `instruments`
table is a trigger-protected 0.21.1 archive and is not used by runtime reads.

## Safety rules

- never modify `main` directly;
- never touch, reset or delete `data/local-agent`;
- do not use `git reset --hard`, `git clean -fd` or `git clean -fdx`;
- preserve screenshots from releases 0.18.0 through 0.21.1;
- use isolated storage roots, process names, ports and Cargo targets;
- use coherent checkpoint commits and push each validated checkpoint;
- never create an empty commit to trigger CI;
- do not merge the future 0.22.0 pull request;
- do not begin the correction-workflow closure planned after 0.22.0.

## Mandatory audit scope

The audit must cover:

- equipment-model aggregate and persistence;
- metrology instrument representation and identity ownership;
- physical-asset APIs, services and UI;
- equipment and metrology migrations;
- station-setup and planned-test material references;
- location derivation from ready station setups;
- correction assignments and metrology history;
- visible catalogue, fleet, metrology, correction and selector vocabulary;
- flat navigation structures and monolithic loading boundaries;
- 0.21.1 migration and compatibility risks.

The target document is:

`docs/domain/equipment-fleet-0.22.0-audit.md`

## Planned checkpoints

1. Start 0.22.0 recovery journal.
2. Audit equipment, metrology, location and executable-material architecture.
3. Define model, physical-asset and metrology boundaries.
4. Introduce authoritative physical-asset persistence.
5. Migrate existing metrology instruments.
6. Add the laboratory-location registry.
7. Split technical-resource navigation and loading boundaries.
8. Build hierarchical model catalogue and fleet.
9. Add model-to-asset creation and asset lifecycle workflows.
10. Attach metrology to physical assets.
11. Integrate station-setup and preparation selectors.
12. Complete vocabulary, accessibility, failure-isolation and visual evidence.
13. Release and validate 0.22.0.
14. Open a pull request for user review without merging it.

## Validation status

Baseline evidence only:

- local and remote `main`: `471e656ab007c1d627456a8dc90d9e3064074f35`;
- `VERSION`: `0.21.1`;
- baseline `CI #130`: `success`;
- working tree clean before branch creation;
- no path under `data/local-agent` modified.

Checkpoint 2 documentation validation:

- mandatory audit scope covered;
- target architecture and migration boundary documented;
- no application, test, migration or screenshot file changed;
- `data/local-agent` untouched.

Checkpoint 3 targeted validation on 2026-07-26:

- `cargo fmt --all`: success;
- `cargo test -p emc-locus-core fleet -- --nocapture`: 6 passed;
- `cargo test -p emc-locus-agent fleet -- --nocapture`: 3 passed;
- the agent workflow test covers two assets on one model, optional serial
  number, exact revision pinning, inventory uniqueness, location rename and
  archive, optimistic concurrency, service/availability transitions, atomic
  audit/outbox evidence and restart persistence;
- `git diff --check`: success before checkpoint publication;
- `data/local-agent` untouched.

Checkpoint 4 targeted validation on 2026-07-26:

- equipment migration 8 and metrology migration 11 apply and verify on fresh
  isolated storage;
- the cross-domain migration is atomic across equipment, metrology and sync;
- the populated 0.21.1 fixture preserves 2 asset IDs, 1 calibration event,
  1 characterization and 1 correction assignment;
- one exact model pin is resolved and one incomplete historical link remains
  explicitly marked `migration_review_required` with its source evidence;
- migration replay keeps 2 assets, 2 audit events, 2 outbox operations and one
  ledger row;
- writes to `legacy_instruments_0_21_1` are rejected;
- metrology service tests: 6 passed;
- full Rust agent tests: 82 passed after the expected 0.22.0 contract updates;
- `cargo clippy -p emc-locus-agent --all-targets -- -D warnings`: success;
- real HTTP metrology restart scenario: passed;
- `data/local-agent` untouched.

Checkpoint 5 validation on 2026-07-26:

- station readiness resolves physical assets from the fleet store and keeps
  metrology limited to calibration and characterization evidence;
- planning creation, rescheduling and historical location identification
  resolve an active registry location inside the SQLite write boundary;
- planned-test preparation snapshots now include category path, inventory
  identity, location, service state, availability and metrology status while
  preserving the canonical shape of historical snapshots through omitted
  defaults;
- LAB CONSOLE exposes `Catalogue des modèles`, `Parc matériel`, `Métrologie du
  parc`, `Montages de mesure`, `Drivers et pilotage`, `Signaux et corrections`
  and `Lieux du laboratoire` as explicit spaces;
- catalogue and fleet trees implement family, subcategory, manufacturer/model
  and physical-item levels with persisted expansion state;
- the asset workflow supports creation without a serial number, identity edits,
  stable-location moves, separate service/availability transitions and exact
  pinned-model navigation;
- the station-setup workflow creates dated drafts and selects only physical
  fleet assets with inventory, serial, location, service, availability and
  metrology context;
- fleet, metrology, driver and location loading errors are isolated to their
  affected panel;
- `cargo fmt --all -- --check`: success;
- `cargo clippy --workspace --all-targets -- -D warnings`: success;
- `cargo test --workspace`: success, including 82 agent tests and 270 core
  tests;
- planned-test preparation service: 6 passed, including real HTTP restart;
- TypeScript typecheck: success;
- ESLint with zero warnings: success;
- Vitest: 27 passed;
- LAB CONSOLE production build: success (only the existing bundle-size
  advisory remains);
- `data/local-agent` untouched.

Checkpoint 6 validation on 2026-07-26:

- the duplicate in-workspace technical navigation was removed; the sidebar is
  now the single primary navigation and repository administration is a
  contextual catalogue action;
- the model wizard no longer asks operators for an internal model ID and keeps
  the generic-model context visible throughout creation;
- visible catalogue, category, driver and model-detail vocabulary was revised
  with natural French labels and correct accents;
- the physical-asset dialog scrolls within the viewport and remains actionable
  at 1280 x 720;
- all planning and preparation seeders and E2E fixtures now create laboratory
  locations through the authoritative registry instead of inventing IDs;
- URL query values are decoded as form data, so catalogue searches containing
  spaces and accented characters work correctly;
- targeted Rust query-decoding test: 1 passed;
- TypeScript typecheck: success;
- ESLint with zero warnings: success;
- Vitest: 27 passed;
- LAB CONSOLE production build: success (existing bundle-size advisory only);
- full real-agent Playwright regression suite: 14 passed in 34.1 seconds;
- `git diff --check`: success;
- `data/local-agent` untouched.

Checkpoint 7 preliminary fleet evidence on 2026-07-27:

- catalogue and fleet identity remain visible when driver or metrology loading
  fails, with a targeted explanation in the affected workspace;
- a dedicated real-agent fleet suite now covers exact model pinning, two
  physical assets on one model, optional serial number, stable location rename,
  asset movement, archived-location refusal, atomic audit/outbox evidence,
  physical-only setup/preparation choices and partial-service failures;
- screenshot writes are guarded by both
  `EMC_LOCUS_REFRESH_RELEASE_SCREENSHOTS=1` and
  `EMC_LOCUS_RELEASE_SCREENSHOT_VERSION=0.22.0`; ordinary tests do not write
  release evidence;
- TypeScript typecheck: success;
- ESLint with zero warnings: success;
- Vitest: 28 passed;
- dedicated real-agent Playwright suite: 5 passed in 9.4 seconds on isolated
  port `8906` and an ephemeral `data/e2e-lab-*` storage root;
- `git diff --check`: success;
- `data/local-agent` untouched;
- the evidence generator remains preliminary and will be extended after the
  external integrity review corrections; no 0.22.0 screenshot has been
  refreshed or accepted yet.

## External integrity review remediation

The external review received on 2026-07-27 is authoritative for the remaining
0.22.0 work. It preserves the equipment-owned fleet architecture but blocks
release readiness until all eight corrections below are implemented and
validated.

| # | Correction | Status | Required proof before completion |
|---|---|---|---|
| 1 | Migrated asset model reconciliation | Complete | Exact CAS command and readable UI; approved/superseded pin, canonical checksum, derived snapshots, replay/mismatch, atomic rollback, restart and real migrated-fixture recovery tested |
| 2 | Administrative availability and computed usage | Complete | Additive migration, manual-state rejection, real reservation/test/setup evidence, precedence, migration and dated selector eligibility covered |
| 3 | Valid asset-creation combinations | Complete | Conditional service reason, forced administrative unavailability, optional location/serial, focus, retained values and no partial backend evidence tested |
| 4 | Authoritative dated metrology status | Complete | Core civil-date assessment, resilient fleet marker and one frontend formatter cover every status; selector contract now carries the same typed assessment |
| 5 | Atomic station/location writes | Complete | `BEGIN IMMEDIATE` boundary attaching equipment and sync, server-derived label, archive concurrency, replay and historical snapshot tests |
| 6 | Identification versus location movement | Complete | Separate commands, edits at archived current locations, active-destination checks, removal, rename display, historical audit, replay and concurrency tested |
| 7 | Eligibility-aware executable selectors | Complete | Shared dated backend read model, setup endpoint and preparation projection explain unresolved, unavailable, conflicting, misplaced and expired assets |
| 8 | Operator comprehension and history | Complete | Permanent model/fleet context, aptitude vocabulary, real fleet audit history, independent model/preparation loading and final vocabulary review covered |

Existing checkpoint 7 tests are supporting evidence only. They do not close
the findings above. The current dedicated suite proves 5 real-agent scenarios;
the reviewed target requires the complete reconciliation, usage, metrology,
atomicity, eligibility and failure-isolation scenarios before screenshot
refresh or version bump.

### Correction 1 validation on 2026-07-27

- dedicated route: `POST /api/v1/fleet/assets/{asset_id}/transitions/reconcile-model`;
- candidate route exposes only validated `approved` or `superseded` immutable
  revisions with readable model, category, version, status and approval date;
- the server recomputes the canonical definition checksum and derives all
  model/category snapshots; clients never supply checksum or category code;
- reconciliation, numeric revision increment, operation record, audit and
  sync outbox are atomic under `BEGIN IMMEDIATE`;
- successful replay returns the original result; replay mismatch, stale asset
  revision, draft revision, corrupt checksum and already-resolved repointing
  are rejected without partial evidence;
- migration evidence remains byte-for-byte unchanged and restart reads retain
  the exact selected revision;
- safe identification, service-state and active-location edits remain possible
  before reconciliation without making the asset resolved;
- the populated 0.21.1 migration fixture proves post-import reconciliation;
- `cargo test -p emc-locus-agent fleet_service --no-fail-fast`: 5 passed;
- populated 0.21.1 migration fixture: 1 passed;
- `cargo test -p emc-locus-agent --lib`: 85 passed;
- `cargo clippy -p emc-locus-agent --all-targets -- -D warnings`: success;
- TypeScript typecheck: success;
- ESLint with zero warnings: success;
- Vitest: 29 passed, including the readable reconciliation dialog and exact
  request contract;
- no migration or historical screenshot was modified;
- `data/local-agent` untouched.

### Correction 2 validation on 2026-07-27

- equipment migration `0009_administrative_availability.sql` separates the
  two fleet-owned administrative values from workflow-owned usage and keeps
  every legacy value as JSON evidence;
- the write API accepts only `available` and `unavailable`, requires a reason
  for manual unavailability, and rejects `reserved`, `assigned_to_setup` and
  `in_test` without mutation, audit or outbox;
- fleet reads compute dated usage with deterministic precedence from technical
  or administrative blocks, active measurement runs, real planned-test
  reservations, and ready montage references;
- every evidence item identifies its authoritative source, readable label,
  interval, reason and blocking effect; montage references remain informative
  rather than exclusive;
- source-database failures preserve the physical identity and add targeted
  non-blocking evidence instead of failing the fleet list;
- LAB CONSOLE separates the administrative editor from calculated usage and
  displays its evidence; no control offers a manually invented reservation,
  montage allocation or active test;
- targeted core fleet tests: 6 passed;
- targeted derived-usage service test: 1 passed;
- targeted every-legacy-state migration test: 1 passed;
- targeted LAB CONSOLE test: 1 passed;
- `cargo fmt --all -- --check`: success;
- `cargo clippy --workspace --all-targets -- -D warnings`: success;
- `cargo test -p emc-locus-agent --lib`: 87 passed;
- `cargo test -p emc-locus-core fleet`: 6 passed;
- TypeScript typecheck and ESLint with zero warnings: success;
- Vitest: 30 passed;
- LAB CONSOLE production build: success, with only the unchanged bundle-size
  advisory;
- `git diff --check`: success;
- `data/local-agent` untouched.

### Corrections 3 and 6 validation on 2026-07-27

- asset creation requires a service-state reason for every state other than
  `usable`; maintenance, out-of-service and retired states are canonicalized
  to administrative unavailability before persistence;
- LAB CONSOLE explains every missing value, focuses the first missing field,
  preserves all entries after backend rejection, and makes serial number and
  laboratory location explicitly optional;
- location-registry failure no longer blocks creation without a location;
- `PUT /api/v1/fleet/assets/{asset_id}/identification` updates only inventory,
  serial, part number, ownership and notes, without loading the current place;
- `POST /api/v1/fleet/assets/{asset_id}/transitions/move` validates an active
  destination under `BEGIN IMMEDIATE`, derives its label server-side, and
  commits revision, audit and outbox atomically;
- assets remain readable and editable at an archived current place; movement
  to an archived place is refused, movement to an active place and explicit
  location removal are supported;
- current views resolve renamed labels from the stable location identity while
  movement audit retains the previous readable snapshots;
- fleet service tests: 8 passed, including creation combinations, archived
  current location, replay, stale concurrent move and restart reads;
- targeted LAB CONSOLE creation and movement tests: 2 passed;
- `cargo fmt --all -- --check`: success;
- `cargo clippy --workspace --all-targets -- -D warnings`: success;
- `cargo test -p emc-locus-agent --lib`: 89 passed;
- TypeScript typecheck and ESLint with zero warnings: success;
- Vitest: 31 passed;
- LAB CONSOLE production build: success, with only the unchanged bundle-size
  advisory;
- `data/local-agent` untouched.

### Correction 4 validation on 2026-07-27

- `emc-locus-core::metrology` owns a serializable civil-date assessment with
  explicit requirement, decision, calibration dates, warning threshold,
  blocking flag and structured reason codes;
- status precedence distinguishes `valid`, `due_soon`, `expired`, `missing`,
  `not_required`, `nonconforming`, `indeterminate` and `unavailable`;
- the fleet endpoint accepts independent `at=<RFC3339>` and
  `checked_on=<YYYY-MM-DD>` values, so metrology dates are not interpreted by
  browser or machine timezones;
- fleet identity reads tolerate an unavailable or invalid metrology database
  and return a blocking `unavailable` marker instead of failing the list;
- preparation snapshots persist the typed assessment rather than the former
  loose `metrology_status` and `calibration_due_at` pair;
- fleet, station setup and preparation use the same LAB CONSOLE formatter;
  no component compares `due_at` with its local clock;
- agent tests: 94 passed after correction, including actual calibration
  assessment on explicit civil dates and metrology-source failure isolation;
- dedicated assessment tests: 5 passed (2 core and 3 agent);
- TypeScript typecheck: success;
- Vitest: 40 passed across the targeted application and status suites,
  including all eight exact operator labels and the no-false-valid guard;
- `cargo clippy --workspace --all-targets -- -D warnings`: success;
- `data/local-agent` untouched.

### Correction 5 validation on 2026-07-27

- station write connections attach `equipment.sqlite` and `sync.sqlite` to
  `station.sqlite`, verify rollback-journal compatibility and open one
  `BEGIN IMMEDIATE` transaction;
- creation, draft replacement, derived-revision creation and the `ready`
  transition load and validate the active laboratory location inside that
  transaction;
- the server derives the current registry label; forged client labels are
  absent from the command contract and cannot alter canonical definitions;
- operation replay is checked inside the write boundary before current
  location validation, so a successful creation remains replayable after a
  later archive;
- deterministic service tests cover concurrent archive during creation and
  draft update, zero partial identity/revision/operation/audit/outbox evidence,
  label override prevention, rename with stable identity and immutable
  historical snapshots;
- the real HTTP restart workflow covers an archived-location refusal during
  the `ready` transition and proves absence of audit/outbox evidence for the
  refused operation;
- `cargo fmt --all -- --check`: success;
- `cargo clippy --workspace --all-targets -- -D warnings`: success;
- Rust agent tests: 97 passed in 87.95 seconds, including the 3 dedicated
  station concurrency/history tests and the real HTTP restart workflow;
- TypeScript typecheck and ESLint with zero warnings: success;
- Vitest: 40 passed, including the physical-asset station workflow;
- LAB CONSOLE production build: success, with only the unchanged bundle-size
  advisory;
- no migration or historical screenshot was modified;
- `data/local-agent` untouched.

### Correction 7 validation on 2026-07-27

- `GET /api/v1/station-setups/asset-options` evaluates physical assets for a
  requested use date, execution mode and stable laboratory location;
- station setup and planned-test preparation consume one read model containing
  category path, manufacturer/model/variant, inventory and serial identity,
  current location, service state, administrative availability, computed
  usage, dated metrology, exact model-link state, eligibility, blocking reasons
  and warnings;
- every reason exposes a stable code, a readable message and a concrete next
  action; eligible choices are sorted first and LAB CONSOLE keeps explained
  non-eligible choices under `Matériels non disponibles`;
- unresolved migrated assets and invalid model pins are blocked; catalogue
  model identities are never returned as physical options;
- maintenance, out-of-service, retired and administrative unavailability are
  blocking; restricted use remains selectable with an explicit warning;
- missing, archived or incompatible locations block selection; exact current
  registry labels remain visible;
- active tests and real overlapping preparations are blocking, while a ready
  montage reference remains non-exclusive evidence;
- preparation excludes only its own schedule reservation, preventing an
  auto-conflict without hiding other reservations or active tests;
- metrology is evaluated on the requested civil date: `not_required` is
  accepted, due-soon is a warning, nonconforming/unavailable is blocking, and
  expired/missing/indeterminate required evidence blocks accredited use;
- schedule instants stored as laboratory civil time (`YYYY-MM-DDTHH:MM`) are
  parsed deterministically and now participate in usage-conflict detection;
- preparation snapshots carry the same operational usage and metrology status
  shown by the option contract, using the schedule date rather than the old
  montage date;
- readiness reuses the backend eligibility rules, so a forged draft cannot
  bypass service, availability, usage, model, location or metrology blocks;
- focused backend matrix: 1 passed, covering eligible, unresolved,
  out-of-service, unavailable, restricted, no-location, wrong-location,
  expired accredited, real reservation, active test and self-reservation
  exclusion cases;
- planned-test preparation service tests: 5 passed;
- real HTTP station workflow includes the option endpoint and restart: passed;
- Rust agent tests: 98 passed in 89.99 seconds;
- `cargo fmt --all`: success;
- `cargo clippy --workspace --all-targets -- -D warnings`: success;
- TypeScript typecheck and ESLint with zero warnings: success;
- Vitest: 40 passed, including disabled ineligible options, next-action detail,
  restricted warning and targeted option-source failure;
- LAB CONSOLE production build: success, with only the unchanged bundle-size
  advisory;
- no migration or historical screenshot was modified;
- `data/local-agent` untouched.

### Correction 8 validation on 2026-07-27

- station preparation now uses laboratory aptitude language throughout:
  `Contrôler l'aptitude du montage`, `Points à corriger avant utilisation` and
  `Montage apte à être utilisé`; the former aviation metaphor is absent from
  the normal operator UI;
- permanent context distinguishes `modèle générique` from `exemplaire du
  parc`, including after navigation between model-detail tabs;
- the fleet history uses the real asset audit endpoint and presents readable,
  newest-first events with date, actor, reason and before/after values;
  operation IDs, revisions, device IDs, correlation IDs and raw payloads are
  retained under collapsed technical details;
- identification, movement, service-state and administrative-availability
  audit evidence expose explicit readable old/new values; a dedicated Rust
  test protects that contract;
- model identity loads independently from revision history and model audit;
  either secondary source can fail without hiding the selected model, and
  stale asynchronous responses cannot replace a newer selection;
- preparation project/schedule context remains visible while material choices
  or preparation history fail independently; prior usable secondary data is
  preserved during a failed refresh;
- navigation and profile vocabulary now says `Profils de pilotage`, with
  operator-facing French labels and technical identifiers moved behind
  optional detail disclosures;
- `cargo fmt --all -- --check`: success;
- `cargo clippy --workspace --all-targets -- -D warnings`: success;
- `cargo test -p emc-locus-agent --lib`: 99 passed;
- TypeScript typecheck: success;
- ESLint with zero warnings: success;
- Vitest: 42 passed, including real history, partial-failure retention,
  permanent context and vocabulary assertions;
- LAB CONSOLE production build: success, with only the existing bundle-size
  advisory;
- `git diff --check`: success before documentation update;
- `data/local-agent` untouched.

### Checkpoint 9 validation on 2026-07-27

- the dedicated real-agent suite now owns eleven serialized workflows for the
  0.22.0 fleet vertical instead of relying on browser mocks;
- physical creation covers a serial number, an explicitly absent serial
  number, an absent location, every service-state reason rule, and rejection
  of manually invented operational usage;
- a direct isolated SQLite fixture represents one unresolved 0.21.1 migrated
  asset; reconciliation pins one exact immutable model revision and proves
  candidate diagnostics, idempotence refusal, audit and outbox evidence;
- location evidence covers rename, explicit movement, archive, an asset still
  readable at an archived current location, identity editing there, refusal to
  move into an archived location and readable movement history;
- dated metrology evidence covers valid, due-soon, expired and nonconforming
  calibration through the authoritative API and LAB CONSOLE labels;
- station and planned-test selectors use physical fleet assets and retain
  operator context when metrology, driver, model-audit or preparation-option
  sources fail independently;
- a real preparation assessment derives `reserved` usage at the planned
  instant, then the real start transition derives `in_test`; the customer test
  object remains distinct from laboratory instrumentation;
- the runner exports only its isolated process and storage metadata; the last
  workflow replaces the actual Local Agent process and reloads fleet, audit and
  outbox state after restart;
- the suite hashes every historical release screenshot from 0.18.0 through
  0.21.1 before execution and verifies the same hashes afterwards;
- complete dedicated Playwright run on port 8911 with
  `data/e2e-lab-e7d1f47ca451468fbbb05ad48fa9d3fd`: 11 passed in 13.8 seconds;
- restart dependency run: 5 passed in 6.9 seconds; focused reservation run:
  1 passed in 4.7 seconds;
- `cargo fmt --all -- --check`: success;
- `cargo clippy --workspace --all-targets -- -D warnings`: success;
- `cargo test -p emc-locus-agent --lib`: 100 passed in 91.72 seconds;
- TypeScript typecheck and ESLint with zero warnings: success;
- Vitest: 42 passed in 27.18 seconds;
- LAB CONSOLE production build: success, with only the existing bundle-size
  advisory;
- `git diff --check`: success before this documentation update;
- every generated `data/e2e-lab-*` root was removed by the guarded runner;
  `data/local-agent` remained untouched.

### Checkpoint 10 validation on 2026-07-28

- the explicit current-release refresh produced 32 screenshots under
  `docs/ux/0.22.0/screenshots`; no historical release image was refreshed;
- principal screens were reviewed as model, physical asset, metrology record,
  station setup, location or planned-test context in
  `docs/ux/0.22.0-equipment-fleet-ux-review.md`;
- the review records each primary action, disabled reason, next action,
  retained technical term and 1440x900/1280x720 result;
- visual inspection found and corrected four release defects: leaked `show`
  and English demo-category labels, wrapped fleet tabs at 1280 pixels, a model
  reconciliation panel enlarged by native-select content, and a disabled
  creation explanation initially below the captured viewport;
- shared operator labels now translate demo-only category/model vocabulary and
  planned material requirements without rewriting immutable domain snapshots;
- the no-location fixture is a real software-license instance pinned to the
  approved acquisition-software model rather than a power-meter model;
- responsive assertions prove no page overflow in the 1280-pixel catalogue,
  no detail overflow in the physical-asset view and no dialog overflow in
  model reconciliation;
- the final guarded refresh used
  `EMC_LOCUS_REFRESH_RELEASE_SCREENSHOTS=1` and
  `EMC_LOCUS_RELEASE_SCREENSHOT_VERSION=0.22.0`, port 8918 and isolated storage
  `data/e2e-lab-884e6c327f924438bd2ec5fe1e050f13`;
- dedicated real-agent Playwright: 11 passed in 18.9 seconds, including actual
  Local Agent restart and historical screenshot hash verification;
- TypeScript typecheck and ESLint with zero warnings: success after the final
  source corrections;
- Vitest: 42 passed;
- LAB CONSOLE production build after those controls: success, with only the
  existing bundle-size advisory;
- `scripts/validate-historical-release-screenshots.ps1`: success against
  baseline `c4e361b17fc37cb22ca209eb0d3f0d238b70673c`;
- the runner removed its generated storage and process state;
  `data/local-agent` remained untouched.

### Interrupted-session recovery and checkpoint 11A on 2026-07-28

- local and remote feature heads both started at
  `0a3fe1467a08b62753a018941b9e0b9baeec6646`; the merge base remained
  `471e656ab007c1d627456a8dc90d9e3064074f35`;
- the interrupted session had valid uncommitted release preparation in 14
  manifest, version and current-contract documentation files; the work was
  inspected, preserved exactly outside the repository while the functional
  checkpoint was isolated, and is reapplied after this checkpoint;
- station material options now carry an explicit key made from
  `planned_use_on`, `execution_mode` and `laboratory_location_id` plus a
  monotonic request sequence;
- changing any context field immediately invalidates the selected asset and
  disables assignment; empty date/location clears options, and only the latest
  matching response can publish selectable assets;
- known asset labels are retained for already saved montage bindings while a
  new context loads or fails; the loading and failure messages remain local to
  the selector and never replace the montage;
- the remaining normal planning placeholders now say
  `Sélectionner un lieu actif du laboratoire`; the historical 0.22.0 audit
  description of the former behavior remains intentionally unchanged;
- Vitest added a deferred-response regression covering A loaded, switch to B,
  late A ignored, only B selectable, blank location clearing and targeted API
  failure while the existing binding remains readable;
- the real-agent suite holds one active-location response, switches back to the
  current context, then releases the stale response and proves it cannot
  replace the current options;
- Rust formatting and strict workspace Clippy: success;
- TypeScript typecheck and ESLint with zero warnings: success;
- Vitest: 43 passed; production build: success with only the existing bundle
  size advisory;
- first real-agent proof correctly found that a previously archived fixture
  location was absent from the active selector; the scenario was corrected to
  create a third active location through the public API;
- dedicated real-agent Playwright after correction: 11 passed in 14.2 seconds
  on port 8922 and isolated storage
  `data/e2e-lab-28e4b09ce64a42459867b4ed2ba95c85`;
- the guarded runner removed both attempted isolated roots and process state;
  `data/local-agent` remained untouched.

### Checkpoints 11B and 11C on 2026-07-28

- every active release source now agrees on `0.22.0`: `VERSION`, Rust workspace
  metadata and lock file, Python package metadata, LAB CONSOLE package and lock
  metadata, and the frontend version source;
- README, CHANGELOG, current storage/API/agent documentation and the 0.22.0
  release note describe the implemented fleet boundary without rewriting
  historical 0.21.1 evidence;
- the Rust agent and LAB CONSOLE production bundle were rebuilt after the
  version bump;
- the guarded real-agent workflow verified both visible version surfaces:
  `LAB CONSOLE 0.22.0` and `Agent local 0.22.0`;
- all 32 current-release PNG files were regenerated after that rebuild with
  port 8924, storage `data/e2e-lab-6c639dce844644938edda5da75c1e8ba`,
  process state `e2e-lab-6c639dce844644938edda5da75c1e8ba.restarted.pid` and
  Cargo target `target/e2e-agent-build`;
- dedicated real-agent Playwright: 11 passed in 19.3 seconds, including Local
  Agent restart and historical screenshot hash protection;
- manual inspection covered every regenerated 1440 x 900 and 1280 x 720
  image; a leaked internal category value `power_meter` was found, translated
  through the shared operator label, protected by an E2E assertion, then the
  complete current-release set was rebuilt and refreshed again;
- the final images expose `Wattmètres RF` rather than the internal code, retain
  explained unavailable station assets, and show no obsolete location
  placeholder;
- historical screenshots from 0.18.0 through 0.21.1 remain protected against
  the baseline `c4e361b17fc37cb22ca209eb0d3f0d238b70673c`;
- generated isolated storage and process state were removed;
  `data/local-agent` remained untouched.

### Remediation order

1. inspect the published 0.22.0 migrations and service transaction boundaries;
2. implement model reconciliation;
3. introduce administrative availability and computed usage;
4. split identification/movement and correct asset creation;
5. centralize dated metrology status;
6. make station/location writes atomic;
7. expose eligibility-aware setup and preparation options;
8. finish vocabulary, real history and independent secondary-panel loading;
9. extend and validate the real-agent evidence suite;
10. review the current UX and refresh only 0.22.0 screenshots;
11. bump to 0.22.0 only after all targeted and visual checks pass.

### Exact resume commands

```powershell
Set-Location 'E:\projets codex\EMC-Locus'
git fetch origin
git switch feature/0.22.0-equipment-fleet-ux-clarity
git pull --ff-only origin feature/0.22.0-equipment-fleet-ux-clarity
git status --short --branch
git rev-parse HEAD
git merge-base HEAD origin/main
Get-Content .\VERSION
Get-Content .\docs\work-in-progress\0.22.0-recovery.md
Get-ChildItem .\storage\sqlite\equipment\migrations
Get-ChildItem .\storage\sqlite\metrology\migrations
Get-ChildItem .\storage\sqlite\station_configurations\migrations
```

Resume at the complete post-version-bump validation matrix. Do not modify a
published migration, historical screenshot or main.

## Complete post-version-bump validation on 2026-07-28

- Rust formatting and workspace Clippy with warnings denied: success;
- Rust workspace tests: 100 agent and 272 core tests passed;
- Python compilation, Qt compilation and complete Python suite: 189 passed;
- migration inventory: equipment 9, measurement data 7, metrology 11, projects
  8, station 2, sync 5, test definitions 5 and update catalog 2;
- TypeScript and ESLint: success; Vitest: 43 passed;
- production build: success with the known 597.69 kB Vite bundle advisory;
- complete real-agent Playwright: 25 passed in 48.9 seconds;
- separate 0.22.0 real-agent restart suite: 11 passed in 13.9 seconds, 18.00
  seconds including isolated setup and cleanup;
- release consistency, historical screenshot validation and launcher smoke:
  success;
- authoritative `scripts/validate-ci.ps1 -NoInstall`: success in 306.56
  seconds;
- exact final acceptance duration including the separate required restart
  suite: 324.56 seconds.

Migration tests cover fresh storage, 2 imported physical assets, 1 calibration,
1 characterization, 1 correction assignment, all pre-0009 administrative
states, restart and idempotent replay. Replay retains 2 fleet audit events, 2
outbox operations and 1 migration-ledger row. One exact model pin remains
exact; one unresolved asset retains its review evidence. Python boundary tests
also retain 3 dossiers and 1 calibration event and refuse archived identity
reads after metrology migration 0011.

The compatible frontend lock update removes the vulnerable production PostCSS
version. Five high-severity npm advisories remain confined to the development
ESLint/minimatch/brace-expansion chain; remediation requires the ESLint 10 major
upgrade and remains explicit tooling debt.

No generated `data/e2e-lab-*` root or process state remains. Historical
screenshots still match baseline
`c4e361b17fc37cb22ca209eb0d3f0d238b70673c`; `data/local-agent` is untouched.

## Remaining work

- publish the final repeatable planning-evidence correction and its review
  record as one candidate commit;
- require successor green GitHub Actions on that exact candidate head;
- stop before merge.

## Pull request and remote CI evidence

- pull request: `#3`, open, non-draft and mergeable into `main`;
- first validated head: `7654cfc128dc4911f91c96457928b91819abda97`;
- workflow: `CI`;
- first green run: number `131`, ID `30373970704`;
- evidence head `fa9bef6bfed7af4a38eba3cc3f0d1103b7a38ad6`: run `132`,
  ID `30377009744`, failed only because the historical-location E2E expected
  the initially selected candidate dossier within 10 seconds;
- correction: the scenario now waits for the initial workspace, selects the
  historical dossier, then performs an explicit candidate-dossier transition
  and verifies its schedule response;
- corrected scenario repetition: 10 passed in 1.5 minutes;
- complete local rerun: 100 agent, 272 core, 189 Python, 43 Vitest and 25
  real-agent Playwright tests passed; the full E2E suite took 50.1 seconds;
- current validated correction head:
  `2d869dcdccf6c5ce48a0842d6fa4c28d6f356545`;
- latest green run: number `133`, ID `30381055102`;
- conclusion: `success`;
- job: `Local validation matrix`, every required step successful;
- evidence head `43d02d9d239ebd19bd28061ef30e04b2f46ef356`: run `134`,
  ID `30383613608`, failed only because `lab-planning.spec.ts` asserted the
  created dossier heading after 10 seconds while its schedule request was still
  pending;
- retained trace evidence: project creation and project, contract-review and
  audit reads succeeded in 5.9 to 13.4 milliseconds; the project schedule read
  had not completed when the assertion ended;
- final correction: wait for the authoritative project schedule response with
  a 30-second bound, and create a unique operator and laboratory location for
  every repetition;
- corrected planning scenario repetition: 10 passed in 26.9 seconds;
- final authoritative local matrix: 100 agent, 272 core, 189 Python, 43 Vitest
  and 25 real-agent Playwright tests passed; the complete E2E suite took 49.6
  seconds; formatting, Clippy, migrations, TypeScript, ESLint, build, release
  consistency, historical screenshots and launcher smoke also passed;
- the pull request is not merged and the feature branch is preserved;
- the final candidate commit requires a successor green run on its exact head
  before final user handoff.

## Release readiness

`READY`

This status is conditional on a successful GitHub Actions `CI` run on the exact
final candidate commit. No further repository commit is required once that
condition is satisfied.
