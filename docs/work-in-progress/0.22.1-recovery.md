# 0.22.1 Station Material Requirements and Calibration - Recovery

## Baseline

- repository: `jiyaomutan-prog/EMC-Locus`;
- baseline branch: `main`;
- baseline release: `0.22.0`;
- inspected local and remote main SHA:
  `e1abe96c388d7f3626415d3a1088dbd8136ab01c`;
- initial feature SHA: `e1abe96c388d7f3626415d3a1088dbd8136ab01c`;
- initial `VERSION`: `0.22.0`;
- feature branch:
  `feature/0.22.1-station-material-requirements-and-calibration`;
- the working tree was clean before branch creation on 2026-07-31;
- no pre-existing 0.22.1 branch was present;
- `data/local-agent` must remain untouched.

## Confirmed Defects

1. Station definition v2 stores only immediate physical asset bindings. The
   candidate endpoint filters design choices through current operational
   eligibility, so an exact but blocked asset cannot be expressed as a setup
   requirement.
2. LAB CONSOLE labels a characterization source as a calibration certificate,
   but exposes no command for the authoritative calibration event already
   supported by the Local Agent. A characterization therefore appears more
   powerful than it is while calibration correctly remains missing.
3. JSON responses use `application/json` without an explicit UTF-8 charset,
   which leaves Windows PowerShell 5.1 free to decode French payloads
   incorrectly.

The detailed pre-change audit is in
`docs/domain/station-material-requirements-0.22.1-audit.md`.

## Architecture Decisions

- Preserve immutable v1 and v2 station revisions and their canonical JSON and
  checksums.
- Introduce
  `emc-locus.station-measurement-setup-definition.v3` as a typed core
  contract.
- Separate `material_requirements`, physical `material_assignments`, and
  contextual operational readiness.
- Use logical ports in requirements and snapshot exact physical port mappings
  only after an immutable model revision and physical asset are selected.
- Match cross-model requirements by stable capability kind and trusted units;
  model-local capability IDs are supporting evidence only.
- Replace the ambiguous lifecycle with `draft`, `qualified`, `ready`, and
  `superseded` for v3 while continuing to read historical v2 `ready`
  revisions.
- Keep matching and readiness authoritative in Rust. TypeScript and Python
  consume projections and never implement a parallel matcher.
- Reuse `calibration_events` and the existing metrology service. The release
  adds a first-class LAB CONSOLE action and client models, not a second
  calibration store.
- Keep calibration events, characterizations, and correction assignments as
  three separate evidence boundaries. No operation implies another.
- Declare all Local Agent JSON success and error responses as
  `application/json; charset=utf-8`.

## Compatibility and Migrations

- Add station migration `0003` for the v3 contract metadata and any required
  lifecycle constraints/indexes without editing migrations 0001 or 0002.
- Preserve every station v2 definition and checksum byte-for-byte.
- Upgrade only a derived editable draft through an explicit audited command;
  never mutate the source v2 revision.
- No metrology schema migration is planned initially because the existing
  `calibration_events` model already carries the required decision, dates,
  as-found/as-left states, uncertainty, traceability, document manifest,
  actor evidence, audit, outbox, and idempotence contract.
- Validate fresh storage, populated 0.22.0 storage, migration replay, restart,
  and v2-to-v3 derivation before release.

## Planned Checkpoints

1. `docs(0.22.1): audit station and calibration workflow gaps`
2. `feat(core): model station material requirements and assignments`
3. `feat(agent): evaluate material requirement candidates`
4. `feat(agent): integrate deferred assignments with preparation`
5. `feat(lab): design station material roles and assignments`
6. `feat(lab): add first-class calibration workflow`
7. `feat(agent): declare JSON responses as UTF-8`
8. `feat(qt): present requirements, assignments and calibration evidence`
9. `test(0.22.1): cover station and metrology integrity`
10. `release(0.22.1): bump versions and publish evidence`

Each coherent checkpoint is checked with `git diff --check`, committed, and
pushed to the feature branch. No empty checkpoint is created.

## Validation Commands

- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- repository Python compilation and unit tests
- `npm ci`, `npm run typecheck`, `npm run lint`, `npm run test`,
  `npm run build`, and `npm run test:e2e` from `apps/lab-console`
- dedicated 0.22.1 real-agent Playwright suite
- `scripts/validate-ci.ps1 -NoInstall`
- `scripts/validate-historical-release-screenshots.ps1`
- fresh and populated migration fixtures plus restart persistence
- `git diff --check` and `git diff --cached --check`

## Current Status

`CORE_AND_AGENT_FOUNDATION_COMPLETE` on 2026-07-31.

- station schema v3 separates typed requirements, assignments, logical ports,
  physical mappings, compatibility and operational eligibility;
- v1/v2 canonical serialization remains unchanged;
- station migration 0003 adds qualification evidence without rebuilding or
  rewriting historical revisions;
- exact blocked assets can be saved and logically qualified, but cannot be
  declared ready without operational assignments;
- a requirement-specific candidate route is wired to authoritative Rust
  matching and fleet eligibility;
- explicit v2-to-v3 derivation preserves the source and requires current
  authoritative asset/model snapshots;
- targeted core station tests: 12 passed;
- targeted station service tests: 4 passed;
- `cargo check --workspace`: success;
- `cargo fmt --all`: applied and `git diff --check` is required before the
  checkpoint commit;
- no UI, version, release screenshot, or operator storage has been changed.

## Exact Resume Instructions

1. Switch to
   `feature/0.22.1-station-material-requirements-and-calibration`.
2. Run `git status --short --branch` and verify `data/local-agent` has no diff.
3. Read this journal and
   `docs/domain/station-material-requirements-0.22.1-audit.md`.
4. Inspect the latest branch log to identify the last published checkpoint.
5. Continue from the first incomplete checkpoint without resetting or
   discarding valid changes.
